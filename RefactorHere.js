


function CalculatePaintNeeded(paintPerUnit, radius) {
  var PI = PI;
  // extract method
  // extract variable
  // rename symbol


  var area;
  area = PI * radius * radius;

  return area / paintPerUnit;
 
}
